{% extends 'base.html.twig' %}

{% block title 'Modifier la recette ' ~ recipe.title %}

{% block body %}
    <div class="container">
        <div class="sidebar-flex">
            <div class="aside-left pr-30-995">
                {{
                include(
                    'admin/recipe/_form.html.twig',
                    {
                        form: form,
                        button: 'Modifier'
                    }
                )
                }}
            </div>

            {{ adminSidebar('admin_recipe') }}
        </div>
    </div>
{% endblock %}







