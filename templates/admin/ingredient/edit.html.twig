{% extends 'base.html.twig' %}

{% block title "Ingrédient " ~ ingredient.name %}

{% block body %}
    <div class="container">
        <div class="sidebar-flex">
            <div class="aside-left pr-30-995">
                {{
                include(
                    'admin/ingredient/_form.html.twig',
                    {
                        form: form,
                        button: 'Modifier'
                    }
                )
                }}
            </div>

            {{ adminSidebar('admin_ingredient') }}
        </div>
    </div>
{% endblock %}







