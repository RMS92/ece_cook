{% extends 'base.html.twig' %}

{% block title 'Créer un nouvel évènement' %}

{% block body %}
    <div class="container">
        <div class="sidebar-flex">
            <div class="aside-left pr-30-995">
                {{
                include(
                    'admin/event/_form.html.twig',
                    {
                        form: form,
                        button: 'Créer'
                    }
                )
                }}
            </div>

            {{ adminSidebar('admin_event') }}
        </div>
    </div>
{% endblock %}



